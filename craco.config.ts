// @ts-ignore
import { CracoAliasPlugin } from 'react-app-alias';

const options = {
    plugins: [
      {
        plugin: CracoAliasPlugin,
        options: {}
      }
    ]
};

export default options;